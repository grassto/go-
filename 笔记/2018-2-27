1.闭包（匿名函数）
    匿名函数是可以分配不同的地址的

2.defer
    在return之后执行

3.defer常常配合匿名函数使用，用来改变函数的命名返回值

4.匿名函数还可以配合 go 关键字来作为 goroutine 使用

5.应用闭包，将函数作为返回值，见当天例子

6.闭包函数可以操作外部函数的局部变量，不管外部函数是否退出

7.一个返回值为另一个函数的函数可以被称为工厂函数

8.使用闭包进行调试
    1）runtime包的Caller函数
    2）log包中的flag参数

9.计算函数的执行时间，time包中的Now()和Sub函数

10.利用内存缓存来提升性能
在进行大量的计算时，提升性能的最直接有效的方式就是避免重复计算。
例：斐波那契数列的计算，可以将第n个数的值存在数组中索引为n的位置，在进行计算前在数组中先找找看有没有计算过，
若计算过，则直接拿来用。